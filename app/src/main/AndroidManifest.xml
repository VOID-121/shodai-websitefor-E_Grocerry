<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shodai">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/shodai"
        android:label="@string/app_name"
        android:roundIcon="@drawable/shodai"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity
            android:name=".categories"
            android:exported="false" />
        <activity android:name=".yrproducts" />
        <activity android:name=".yorder" />
        <activity android:name=".signup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.google.firebase.auth.internal.FederatedSignInActivity"
            android:launchMode="standard"
            tools:replace="android:launchMode" />
        <activity android:name=".seller" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Signin" />
        <activity android:name=".ProductDetails" />
        <activity android:name=".Cart" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_l7x9oScajUO2rC" />
    </application>

</manifest>